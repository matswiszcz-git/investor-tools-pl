import{describe,it,expect,vi}from'vitest';import{NbpAdapter,previousCalendarDay}from'../nbp/adapter';
describe('NBP adapter',()=>{it('business day uses previous day',async()=>{const get=vi.fn().mockResolvedValue({rates:[{mid:4,effectiveDate:'2026-09-10'}]});await new NbpAdapter({get}).previousBusinessRate('USD','2026-09-11');expect(get).toHaveBeenCalledWith(expect.stringContaining('2026-09-10'))});it('weekend walks back to Friday',async()=>{const get=vi.fn().mockRejectedValueOnce(new Error('404')).mockRejectedValueOnce(new Error('404')).mockResolvedValue({rates:[{mid:4,effectiveDate:'2026-09-11'}]});expect((await new NbpAdapter({get}).previousBusinessRate('USD','2026-09-14')).effectiveDate).toBe('2026-09-11')});it('missing table exhausts window',async()=>{const get=vi.fn().mockRejectedValue(new Error('404'));await expect(new NbpAdapter({get}).previousBusinessRate('EUR','2026-09-14')).rejects.toThrow('Brak tabeli')});it('API error is explicit',async()=>{const get=vi.fn().mockRejectedValue(new Error('500'));await expect(new NbpAdapter({get}).previousBusinessRate('GBP','2026-09-14')).rejects.toThrow('Nie udało')});it('date helper',()=>expect(previousCalendarDay('2026-01-01')).toBe('2025-12-31'))});
