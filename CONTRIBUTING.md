# Contributing

Utwórz branch, dodaj testy bez live API i uruchom `npm run typecheck && npm test && npm run build`. Zmiana reguły podatkowej wymaga oficjalnego źródła oraz aktualizacji wersjonowanego wpisu i metodologii. Nie dodawaj danych prywatnych ani sekretów.
